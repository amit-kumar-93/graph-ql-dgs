spring:
  graphql:
    graphiql:
      enabled: true
      path: /graphiql

management:
  endpoints:
    web:
      exposure:
        include: "*"

#logging:
#  level:
#    root: DEBUG

dgs:
  reload: true
  graphql:
    websocket:
      path: /subscriptions
      connection-init-timeout: 20s